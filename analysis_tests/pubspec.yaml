name: oref_analyzer_tests
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  analyzer: ^14.4.0
  analyzer_testing: ^0.4.2
  oref:
    path: ..
  test: ^1.32.0
  test_reflective_loader: ^0.4.0

dev_dependencies:
  lints: ^6.1.0
