name: oref_examples
description: "Oref examples app"
publish_to: "none"

version: 1.0.0+1

resolution: workspace
environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  oref: any
  oxy: ^0.0.4
  shared_preferences: ^2.5.3

flutter:
  uses-material-design: true
