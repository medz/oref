name: oref_examples
description: "Oref examples app"
publish_to: "none"

version: 1.0.0+1

resolution: workspace
environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  oref: any
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
